{
  "name": "tripreport",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "lint": "npm --prefix app run lint",
    "kill-dev": "powershell -ExecutionPolicy Bypass -File scripts/kill-dev.ps1"
  }
}
